Home(Shows some Hold, some Random items)->
    Browse Items(Show all items, current price only)->
        Show 1 item (Has button to add item to Hold, show 3 historical price?)
    Hold(Shows saved items, has button to remove item from hold) ->
        Show 1 item (Has button to remove from hold) 
    Recipe (Item + process/item = item) ->
        Add Recipe
        Check Recipe (Should check if Airtable contains Item in Input's half, else output is current price from API)
        Delete Recipe